The team should follow a Git Branch Workflow. Each teammate should create a separate branch for their work instead of
 pushing directly to the main branch. After testing, they should create a Pull Request for code review before merging into main.

Git Commands:

1.git checkout -b feature-name  Creates a new branch for a feature.
2.git add . ? Stages the changed files.
3.git commit -m "message"  Saves the changes with a message.
4.git push origin feature-name  Pushes the feature branch to GitHub.

This workflow prevents incomplete or untested code from directly breaking the main branch.
